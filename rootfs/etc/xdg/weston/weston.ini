# Конфиг weston для минимальной системы.
[core]
# Программный рендер (pixman) через DRM dumb-буферы — без Mesa/GBM/EGL/LLVM.
# Для VM без 3D-ускорения это самый лёгкий путь: не нужны ~150 МБ libgallium+libLLVM.
renderer=pixman
